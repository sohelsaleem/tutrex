{% form_theme form 'bootstrap_3_layout.html.twig' %}

{{ form_start(form) }}
{{ form_row(form.name) }}
{{ form_row(form.email) }}
{{ form_row(form.plainPassword) }}
{{ form_row(form.phone) }}
{{ form_errors(form) }}
<div class="modal-buttons text-center">
    <button type="submit" class="btn learnium-secondary-btn register-button">Register</button>
</div>
{{ form_end(form) }}
